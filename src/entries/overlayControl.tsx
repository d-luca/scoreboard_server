import React from "react";
import ReactDOM from "react-dom/client";
import "../global.css";

/** Placeholder for the overlay control window (planned, see docs/features/overlay.md). */
ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
	<React.StrictMode>
		<div className="font-[Poppins] text-2xl text-white">overlay-control entry — not implemented</div>
	</React.StrictMode>,
);
