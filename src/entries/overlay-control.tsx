import React from "react";
import ReactDOM from "react-dom/client";
import "../global.css";

/** Placeholder for the overlay control window (Phase 7 [OPTIONAL], doc 05). */
ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
	<React.StrictMode>
		<div className="font-[Poppins] text-2xl text-white">overlay-control entry — Phase 7</div>
	</React.StrictMode>,
);
