import React from "react";
import ReactDOM from "react-dom/client";
import "../global.css";

/** Placeholder for the Recording window (Phase 8 [OPTIONAL], doc 06 Part A). */
ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
	<React.StrictMode>
		<div className="font-[Poppins] text-2xl text-white">recording entry — Phase 8</div>
	</React.StrictMode>,
);
