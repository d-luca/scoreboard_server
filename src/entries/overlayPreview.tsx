import React from "react";
import ReactDOM from "react-dom/client";
import "../scoreboard.css";

/** Placeholder for the click-through overlay preview window (planned, see docs/features/overlay.md). */
ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
	<React.StrictMode>
		<div className="font-[Anton] text-2xl text-lime-400">overlay-preview entry — not implemented</div>
	</React.StrictMode>,
);
