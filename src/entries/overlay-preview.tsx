import React from "react";
import ReactDOM from "react-dom/client";
import "../scoreboard.css";

/** Placeholder for the click-through overlay preview window (Phase 7 [OPTIONAL], doc 05). */
ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
	<React.StrictMode>
		<div className="font-[Anton] text-2xl text-lime-400">overlay-preview entry — Phase 7</div>
	</React.StrictMode>,
);
